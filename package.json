{
  "name": "meta-healthy-meals",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de META Healthy Meals — generado con un script de build propio (sin frameworks de frontend), con panel de administración en /admin (Decap CMS).",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx serve dist"
  },
  "dependencies": {}
}
